Before the fixes, changing only op for a=5,b=3 left result=8 rather than 2. Changing operands for subtraction to (8,2) and (3,7) produced x instead of 6 and 12. The full test passed 289/516 checks. Add op to the explicit sensitivity list, and use blocking assignments for all three dependent subtraction statements. The unchanged test then passes all 516 checks, including all 512 input/operation combinations. Four-bit results wrap modulo 16.
